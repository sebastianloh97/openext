---
description: Initialize or refresh the project's design package (design system, tokens, kit, framework bridges) in design/
---

Load the `design-init` skill and follow its instructions to initialize or refresh the project's design package.

Direction for this run (optional — e.g. a refresh mode such as audit, re-theme, extend, or import references): $ARGUMENTS
