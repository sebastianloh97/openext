### Runtime Session Info

Use `bun .opencode/scripts/session-info.ts` script to get information:
- session info: datetime, **session id**, server url
- workspace info: directory, worktree, project id, project worktree, project created at, etc.
- model info: provider id, model id, model name, context limits, etc.
